import React from 'react'
import Home from '@/components/Home/Home';

const HomePage = () => {
  return (
    <div>
      <Home />
      {/* Tours Section */}
      {/* The component is actually rendered inside Home.tsx or page.tsx? 
         Wait, I see Home component being used in page.tsx. 
         Let me check Home.tsx first.
      */}
    </div>
  )
}

export default HomePage;
